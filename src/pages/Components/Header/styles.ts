import { styled } from '../../styles';

export const HeaderContainer = styled('header', {
    display: 'flex',
    width: '100%',
    maxWidth: 1180,
    margin: '0 auto',
    justifyContent: 'space-between',
    padding: '2rem 0'
})
export const ImageIgnite = styled('div', {
    display: 'flex',
    margin: '0 auto',
    alignItems: 'flex-start',
})